#pragma config POSCMOD = XT
#pragma config OSCIOFNC = ON
#pragma config FCKSM = CSDCMD
#pragma config FNOSC = PRI
#pragma config IESO = ON
#pragma config WDTPS = PS32768
#pragma config FWPSA = PR128
#pragma config WINDIS = ON
#pragma config FWDTEN = ON
#pragma config ICS = PGx2
#pragma config GWRP = OFF
#pragma config GCP = OFF
#pragma config JTAGEN = OFF

#include "xc.h"
#include "libpic30.h"
#include "adc.h"
#include "buttons.h"
#include <stdlib.h>

void configure_system(void) {
    TRISA = 0x0000; // Все порты PORTA как выходы
    TRISB |= (1 << 3) | (1 << 4); // Порты RB3 и RB4 как входы
    TRISD |= (1 << 13); // RD13 как вход (если используется)
    AD1PCFG = 0xFFFB; // Аналоговые порты конфигурируются соответствующим образом
    ADC_SetConfiguration(ADC_CONFIGURATION_DEFAULT);
    ADC_ChannelEnable(ADC_CHANNEL_POTENTIOMETER);
    LATA = 0x0000; // Инициализация всех выходов PORTA в 0
}

int main(void) {
    uint8_t output_state = 0x00;
    int is_alarm_triggered = 0;
    int is_blinking_phase = 0;
    unsigned long blink_toggle_counter = 0;
    unsigned long blink_timer = 0;

    configure_system();

    while (1) {
        unsigned int pot_value = ADC_Read10bit(ADC_CHANNEL_POTENTIOMETER);
        if (pot_value == 0xFFFF) continue;

        if (!is_alarm_triggered && pot_value < 512) {
            is_alarm_triggered = 1;
            is_blinking_phase = 1;
            blink_toggle_counter = 0;
            blink_timer = 0;
        }

        if (is_alarm_triggered) {
            if (pot_value >= 512 || (PORTD & (1 << 13)) == 0) {
                is_alarm_triggered = 0;
                LATA = 0x00;
                continue;
            }

            if (is_blinking_phase) {
                if (blink_toggle_counter % 2) {
                    LATA = 0b00010000; // Включаем только RA4 (5-й светодиод)
                } else {
                    LATA = 0x00;
                }

                __delay32(1000000);
                blink_toggle_counter++;
                blink_timer += 1000000;

                if (blink_timer >= 5000000) {
                    is_blinking_phase = 0;
                    LATA = 0b00010000; // Постоянно включаем RA4 после мигания
                }
            } else {
                LATA = 0b00010000; // Постоянно включен RA4
            }
        } else {
            LATA = 0x00;
        }
    }

    return 0;
}
